import { createSlice } from "@reduxjs/toolkit";

const foodDataSlice = createSlice({
  name: "foodData",
  initialState: {},
  reducers: {},
});

export const {} = foodDataSlice.actions;

export default foodDataSlice.reducer;
